#include <bits/stdc++.h>
using namespace std;
int main (){
	string s;
	cin>>s;
	int len = s.length();
	int a[200];
	for(int i=0;i<len;i++) a[i] = s[i] - '0';
	for(int i=len-1;i >= 0 ;i--){
		cout<<a[i];
	}
    return 0;
}
