#include <bits/stdc++.h>
using namespace std;

int main (){
    int t;
    cin>>t;
    while(t--){
        char s;
        cin>>s;
        if(s <= 'Z' && s >= 'A') s = tolower(s);
        else s = toupper(s);
        cout<<s<<endl;
    }
    return 0;
}
